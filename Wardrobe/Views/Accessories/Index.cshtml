@model IEnumerable<Wardrobe.Models.Accessory>

@{
    ViewBag.Title = "Index of Accessories";
}

<h2 class="leader">Index of Accessories</h2>

<p class="btn btn-default">
    @Html.ActionLink("Create New", "Create")
</p>
<p></p>
<div class="row clothescontainer">
    @foreach (var item in Model)
    {
        <div class="col-sm-6 col-md-4 ">
            <div class="thumbnail reviewthumb">
                <div class="caption">
                    <p> @Html.DisplayFor(modelItem => item.AccName)</p>
                    <p>@Html.DisplayNameFor(model => model.AccColor):@Html.DisplayFor(modelItem => item.AccColor)</p>
                    <p>@Html.DisplayNameFor(model => model.Occasion.OccName):@Html.DisplayFor(modelItem => item.Occasion.OccName)</p>
                    <p>@Html.DisplayNameFor(model => model.Season.SeasonName): @Html.DisplayFor(modelItem => item.Season.SeasonName)</p>
                    <p> <img class="clothespix" src="@Html.DisplayFor(modelItem => item.AccPic)" /></p>
                    <p class="btn btn-default">
                        @Html.ActionLink("Edit", "Edit", new { id = item.AccID }) |
                        @Html.ActionLink("Details", "Details", new { id = item.AccID }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.AccID })
                    </p>
                </div>
            </div>
        </div>
    }
</div>

