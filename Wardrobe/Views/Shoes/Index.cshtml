@model IEnumerable<Wardrobe.Models.Sho>

@{
    ViewBag.Title = "Index";
}

<h2 class="leader">Index of Shoes</h2>

<p class="btn btn-default">
    @Html.ActionLink("Create New", "Create")
</p>
<p></p>
<div class="row clothescontainer">
    @foreach (var item in Model)
    {
        <div class="col-sm-6 col-md-4 ">
            <div class="thumbnail reviewthumb">
                <div class="caption">
                    <p> @Html.DisplayNameFor(model => model.ShoeName):@Html.DisplayFor(modelItem => item.ShoeName)</p>
                    <p>@Html.DisplayNameFor(model => model.ShoeColor):@Html.DisplayFor(modelItem => item.ShoeColor)</p>
                    <p>@Html.DisplayNameFor(model => model.Occasion.OccName):@Html.DisplayFor(modelItem => item.Occasion.OccName)</p>
                    <p>@Html.DisplayNameFor(model => model.Season.SeasonName): @Html.DisplayFor(modelItem => item.Season.SeasonName)</p>
                    <p> <img class="clothespix" src="@Html.DisplayFor(modelItem => item.ShoePic)" /></p>
                    <p class="btn btn-default">
                        @Html.ActionLink("Edit", "Edit", new { id = item.ShoeID }) |
                        @Html.ActionLink("Details", "Details", new { id = item.ShoeID }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.ShoeID })
                    </p>
                </div>
            </div>
        </div>
    }
</div>

